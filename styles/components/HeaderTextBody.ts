import styled from '../styled-components';

export const HeaderTextBody = styled.p`
  color: ${(props: any) => props.theme.offWhite};
  font-size: 2rem;
  max-width: 33vw;
  letter-spacing: 0.2rem;
  text-align: center;
`;
